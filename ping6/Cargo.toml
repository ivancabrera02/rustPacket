[package]
name = "ping6_rs"
version = "0.1.0"
edition = "2021"
description = "ICMPv6 ping in pure Rust using raw sockets"

[[bin]]
name = "ping6_rs"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
